export PRJ_HOME="$HOME/development/personal/nest_prjcts/prueba_dynamo_nest_js"
alias startDkcLocal="dkc --env-file .env.local up -d --no-recreate dynamodb-admin"
alias stopDkc="dkc --env-file .env.local stop"
alias debug_local="yarn start:debug:local"
alias debug_dev="yarn start:debug:dev"

# need to have a .env.prod with

# AWS_ID=<your id>
# AWS_SECRET=<your secret>
# AWS_REGION=< a region>
# https://docs.aws.amazon.com/general/latest/gr/ddb.html
# AWS_ENDPOINT=https://dynamodb.<your region>.amazonaws.com


